/**
 * @param {string} s
 * @return {number}
 */
 var minOperations = function(s) {
    let operations = 0;

    if (s.length === 1) return operations;

    let idx = 0;

    while (idx < s.length) {
        
    }
};

minOperations('0100');